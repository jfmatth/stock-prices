apiVersion: v1
kind: Service
metadata:
  name: {{ include "stock-prices.fullname" . }}
  labels:
    {{- include "stock-prices.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "stock-prices.selectorLabels" . | nindent 4 }}
