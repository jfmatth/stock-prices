apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "stock-prices.fullname" . }}-test-connection"
  labels:
    {{- include "stock-prices.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "stock-prices.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
